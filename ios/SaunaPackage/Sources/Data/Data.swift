import Foundation

/// Placeholder. Real repositories (`KeychainNicknameStore`,
/// `RemoteRoomRepository`, `WebAuthnAuthenticator`) land in Phase 2.
public enum DataLayer {
    public static let moduleName = "Data"
}
