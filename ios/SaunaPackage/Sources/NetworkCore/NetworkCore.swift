import Foundation

/// Placeholder so SPM has at least one source file. Real `NetworkClient`
/// arrives in the same module once Phase 2 wiring lands.
public enum NetworkCore {
    public static let moduleName = "NetworkCore"
}
